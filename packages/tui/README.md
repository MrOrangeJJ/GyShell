# @gyshell/tui-core

Deprecated and unsupported core TUI runtime implementation for historical `gyll` source. The current command-only client lives in `packages/cli`.

Desktop builds no longer bundle or install this TUI.
