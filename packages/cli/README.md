# `@gyshell/cli-core`

Implementation for the command-only GyShell CLI. It owns argument parsing,
machine-readable output, WebSocket RPC transport, command dispatch, and
agent-oriented wait semantics. It intentionally has no TUI or Electron code.
