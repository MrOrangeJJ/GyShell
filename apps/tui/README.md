# @gyshell/tui

This historical interactive TUI workspace is deprecated and unsupported. The current command-only `gyll` client lives in `apps/cli`.

Desktop builds no longer bundle this workspace, no longer generate `apps/electron/cli-runtime`, and no longer install shell launchers. The remaining files are retained only for historical source reference.
